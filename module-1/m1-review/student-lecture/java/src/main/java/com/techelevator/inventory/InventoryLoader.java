package com.techelevator.inventory;

import java.util.HashMap;
import java.util.Map;

/**
 * 
 * @author Java Green
 *
 */
public class InventoryLoader {
	
	//TODO:
	
	public Map<String, InventoryItem> loadInventoryFromFile(String fileName) {
		
		Map<String, InventoryItem> tempMap = new HashMap<String, InventoryItem>();
		
		
		//create a File object
		//create a scanner (uses file not System.in)
		
		// while the file has next line {
		
		    //Read the next line into a String ( nextLine()		
		
		    //use split command to turn into array of values/fields
		    // create a new InventoryItem and populate it with fields from above array
		    // add the inventory Item to the tempMap, using itemCode as the key
		
		
		// }
		
		
		return tempMap;
		
	}

}
